ln -snf "$(cd $(dirname $0) && pwd)/.zshrc" ~/
ln -snf "$(cd $(dirname $0) && pwd)/.emacs.d" ~/
ln -snf "$(cd $(dirname $0) && pwd)/.tmux.conf" ~/
ln -snf "$(cd $(dirname $0) && pwd)/.screenrc" ~/
ln -snf "$(cd $(dirname $0) && pwd)/.vimrc" ~/
